Scanner sc = new Scanner(System.in);
                int n = sc.nextInt();
                int count = 0;
                for(int i=0; i<32; i++){
                        int bitmask = 1<<i;
                        if((bitmask & n) != 0){
                                count++;
                        }
                }
                System.out.print(count);
